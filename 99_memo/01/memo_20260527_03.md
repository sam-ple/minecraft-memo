Paperサーバーで「ロビー → ミニゲームワールドへ移動」を作るなら、初心者でも安定するのはこの構成です。

---

# おすすめ構成

* サーバー: Paper
* 複数ワールド管理: Multiverse-Core
* ロビー用コマンド: EssentialsX
* GUI移動（任意）: DeluxeMenus

---

# 一番簡単な構成

## イメージ

```text
[ロビー(world)]
   ↓
NPC / 圧力板 / GUI / コマンド
   ↓
[minigame1]
[minigame2]
[pvp]
```

---

# ① Multiverse-Core導入

## plugins に入れる

* Multiverse-Core
* Multiverse-Portals（後で便利）

公式:

* [Paper公式](https://papermc.io/?utm_source=chatgpt.com)
* [Multiverse-Core](https://dev.bukkit.org/projects/multiverse-core?utm_source=chatgpt.com)

---

# ② ワールド作成

サーバーコンソール or OP権限で

```mcfunction
/mv create lobby normal
/mv create pvp normal
/mv create minigame normal
```

確認:

```mcfunction
/mv list
```

---

# ③ ロビーをデフォルトスポーンにする

```mcfunction
/mv setspawn
```

または

```mcfunction
/setworldspawn
```

---

# ④ ワールド移動

## コマンド移動

```mcfunction
/mvtp pvp
```

または

```mcfunction
/mvtp minigame
```

---

# ⑤ 圧力板で移動（簡単）

## コマンドブロック

```mcfunction
/mvtp @p pvp
```

を設定。

その上に感圧板。

---

# ⑥ ロビーアイテムで移動（おすすめ）

EssentialsX + DeluxeMenus で

* コンパスクリック
* GUI表示
* ワールド選択

みたいな「Hypixel風」ができる。

---

# ⑦ ポータル移動（超おすすめ）

## Multiverse-Portals

導入後:

```mcfunction
/mvp create pvpportal
```

木の斧で範囲選択して

```mcfunction
/mvp modify dest w:pvp
```

これでポータルに入るだけで移動。

---

# ⑧ ロビー専用設定（重要）

## ロビーでは

```mcfunction
/gamerule doMobSpawning false
/gamerule keepInventory true
/gamerule doDaylightCycle false
```

---

# ⑨ ワールドごとに設定変更

## PVPだけON

```mcfunction
/mv modify set pvp true pvp
```

## ロビーだけ飛行ON

```mcfunction
/mv modify set allow-flight true lobby
```

---

# ⑩ ワールドリセットも可能

以前聞いていた「試合後に地形を戻す」はこれ。

## 方法

### 小規模なら

* worldフォルダ削除
* テンプレworldコピー
* 再読込

### プラグインなら

* MultiWorld
* AdvancedSlimeWorldManager

など。

---

# ミニゲーム系でよくある構成

```text
lobby
 ├ survival_event_1
 ├ pvp_arena
 ├ tnt_run
 ├ practice
 └ resource
```

試合終了後:

```text
全員 → lobbyへ送還
↓
arena world リセット
↓
次試合
```

---

# あなたの構成ならおすすめ

以前の話から、

* Mac M1
* Paper
* execute多用
* ミニゲーム系
* Skriptも興味あり

なのでかなり相性良い。

特におすすめ:

* Paper
* Multiverse
* Skript
* FastAsyncWorldEdit
* spark

この辺。

---

# さらに進めるなら

次にできるのは:

* ロビー専用アイテム
* NPCクリック移動
* 自動マッチング
* 試合開始カウントダウン
* ワールド自動リセット
* BedWars風
* TNT Run風
* Hypixel風GUI

など。

必要なら次に、

* 「ロビー完成までの具体的手順」
* 「Skriptでワールド移動」
* 「NPCクリックで移動」
* 「試合終了→自動リセット」

も作れる。

* * * * * *

はい、Paper + Multiverse-Core だと、かなり細かく「ワールドごと設定」ができます。

特にミニゲーム運営だと、

* ロビー
* PVP
* TNT RUN
* 建築
* イベント

で全部設定を変えるのが普通です。

---

# まず全体像

```text
server/
├ world/           ← ロビー
├ pvp/
├ tnt_run/
├ build/
├ plugins/
├ paper-world.yml
├ server.properties
└ spigot.yml
```

Minecraftは「ワールド = フォルダ」です。

---

# 実際のフォルダ構成（おすすめ）

```text
server/
├ worlds/
│  ├ lobby/
│  ├ pvp/
│  ├ tnt/
│  ├ practice/
│  └ templates/
│      ├ pvp_template/
│      └ tnt_template/
│
├ plugins/
│  ├ Multiverse-Core/
│  ├ Skript/
│  ├ Essentials/
│  └ FastAsyncWorldEdit/
│
├ cache/
├ logs/
└ paper.yml
```

※ `worlds/` にまとめる人も多い。

---

# ワールドごとに変えられる設定

かなり多い。

---

# ① PVP ON/OFF

## ロビー

```mcfunction
/mv modify set pvp false lobby
```

## PVPワールド

```mcfunction
/mv modify set pvp true pvp
```

---

# ② ゲームモード固定

## ロビーはAdventure

```mcfunction
/mv modify set gamemode adventure lobby
```

## 建築はCreative

```mcfunction
/mv modify set gamemode creative build
```

---

# ③ モンスター湧き

```mcfunction
/mv modify set monsters false lobby
```

---

# ④ 飛行許可

```mcfunction
/mv modify set allow-flight true lobby
```

---

# ⑤ 時間固定

```mcfunction
/gamerule doDaylightCycle false
/time set day
```

---

# ⑥ インベントリ保持

```mcfunction
/gamerule keepInventory true
```

---

# ⑦ 難易度変更

```mcfunction
/difficulty peaceful
```

ワールド別にしたい場合は追加プラグインを使う。

---

# ⑧ ワールド境界

```mcfunction
/worldborder set 500
```

---

# ⑨ コマンド制限

Skriptや権限管理で:

* ロビーだけ `/fly`
* PVPだけ `/spawn`
* buildだけWorldEdit

とか可能。

---

# ⑩ ワールド別インベントリ

超重要。

## これがないと

ロビーのアイテムをPVPへ持ち込める。

---

## おすすめ

Multiverse-Inventories

これで:

```text
ロビー用インベントリ
PVP用インベントリ
建築用インベントリ
```

を分離可能。

---

# ミニゲーム運営で超重要な考え方

## 「ロビー」は特別扱い

普通は:

| 項目     | ロビー       |
| ------ | --------- |
| PVP    | OFF       |
| 建築     | OFF       |
| 飛行     | ON        |
| 時間     | 昼固定       |
| モンスター  | OFF       |
| インベントリ | 専用        |
| ゲームモード | Adventure |

---

# おすすめプラグイン構成

## 基本

* Multiverse-Core
* Multiverse-Portals
* Multiverse-Inventories

---

## かなり便利

* FastAsyncWorldEdit
* WorldGuard
* Skript

---

# WorldGuardが強い

例えばロビーだけ:

* ブロック破壊禁止
* TNT禁止
* PVP禁止
* ドロップ禁止
* エンダーパール禁止

全部できる。

---

# ミニゲーム系の定番構成

```text
lobby
├ queue_pvp
├ queue_tnt
├ pvp_map_01
├ pvp_map_02
├ tnt_map_01
└ build
```

---

# ワールドリセット時の構成

かなり重要。

## テンプレート保存

```text
templates/
└ pvp_template/
```

試合終了時:

```text
pvp_map_01 削除
↓
templateコピー
↓
再生成
```

これがHypixel系の基本。

---

# あなたの環境だとかなり向いてる

以前の話を見ると、

* execute大量
* ミニゲーム志向
* Paper
* M1 Mac
* playit.gg
* Skript興味あり

なので、かなり「マルチワールド型」に向いてる。

特に:

```text
Skript + Multiverse + WorldGuard
```

は最強クラスに相性良い。

---

# 次におすすめ

必要なら次に:

* 「理想的なフォルダ構成」
* 「Hypixel風ロビー」
* 「試合終了→自動リセット」
* 「Skriptでワールド移動」
* 「NPCクリックで移動」
* 「PVP開始システム」

を実際のコード込みで作れる。

* * * * * *

6GBでも十分できます。
ただし「何をやるか」で快適度がかなり変わります。

あなたの構成（Paper + ミニゲーム + execute多用）だと、かなり現実的です。

---

# まず結論

## 6GBで余裕

* ロビー
* PVP
* TNT Run
* Skript
* Multiverse
* Essentials
* WorldGuard
* 5〜15人程度

この辺なら普通に動く可能性高い。

---

# 逆に重くなる要素

特に重いのはこれ。

| 重い要素          | 重さ  |
| ------------- | --- |
| 大量execute     | 高   |
| 常時scoreboard  | 高   |
| 常時範囲検索        | 高   |
| Mob大量         | 高   |
| Hopper大量      | 高   |
| WorldEdit大量処理 | 高   |
| MOD大量         | 高   |
| Chunk読み込み     | 高   |
| Dynmap        | 超重い |

---

# 実は「メモリ」よりCPUが重要

特にMinecraftは:

* メインスレッド依存
* tick処理依存

なので、

```text
TPSが下がる
```

原因はCPUのことが多い。

---

# M1 Macはかなり強い

以前の話だと:

* Mac M1
* 16GB
* 自分が起動時のみ

なので、かなり有利。

M1はMinecraftサーバー性能がかなり良い。

---

# 6GBでおすすめ設定

## Java起動

```bash
java -Xms4G -Xmx6G -jar paper.jar nogui
```

または安定重視なら:

```bash
java -Xms3G -Xmx5G -jar paper.jar nogui
```

---

# 実際の配分イメージ

| 用途        | 使用量    |
| --------- | ------ |
| Paper本体   | 1〜2GB  |
| ワールド複数    | +500MB |
| Skript    | +300MB |
| execute大量 | +500MB |
| プレイヤー増加   | +可変    |

---

# かなり重要な最適化

## spark入れる

spark

超おすすめ。

---

# TPS確認

```mcfunction
/tps
```

---

# sparkで原因分析

```mcfunction
/spark profiler start
```

数分後:

```mcfunction
/spark profiler stop
```

どこが重いか全部見える。

---

# execute多用するなら重要

## 悪い例

毎tick:

```mcfunction
execute as @a at @s run ...
```

を大量に回す。

---

## 良い例

* 範囲限定
* tag限定
* 必要時のみ実行

---

# ワールド数は意外と問題ない

多くの人が勘違いするけど、

```text
ワールド数 ≠ 重さ
```

本当に重いのは:

```text
読み込まれてるChunk数
```

---

# なのでおすすめ

## ロビー以外は空なら停止

```mcfunction
/mv unload worldname
```

でアンロード可能。

---

# ミニゲーム系でおすすめ構成

## 常時ロード

```text
lobby
```

のみ。

---

## 試合時だけロード

```text
pvp_map_01
tnt_map_01
```

---

# World reset方式は軽い

Hypixel系は:

```text
テンプレートコピー
↓
ワールド読み込み
↓
試合
↓
削除
```

これ。

かなり効率良い。

---

# 6GBで危険ライン

## 少し厳しい

* MOD大量
* 20人超
* 常時Mob大量
* Create Mod
* 大規模工業MOD
* Dynmap
* 影MOD前提

---

# あなたの用途ならかなり現実的

特に:

* ミニゲーム
* execute
* ロビー制
* ワールド分離

はPaperと相性良い。

むしろ:

```text
1巨大ワールド
```

より軽くなる場合もある。

---

# 個人的におすすめ構成

## plugins

* Paper
* Multiverse-Core
* WorldGuard
* FastAsyncWorldEdit
* spark
* Skript

これでかなり強い。

* * * * * *

8GBにしても全然OKです。
むしろ、あなたの用途だと「6GBでも動くけど、8GBの方が安心」という感じ。

---

# まず結論

## あなたの用途ならおすすめ

```text
Xms4G
Xmx8G
```

または

```text
Xms6G
Xmx8G
```

くらい。

---

# ただし「多ければ多いほど良い」ではない

Minecraft(Java)は、

```text
メモリ増やしすぎ
↓
GC（ガベージコレクション）重くなる
↓
逆にカクつく
```

ことがある。

---

# でもM1 + 16GBならかなり余裕

以前の話だと:

* Mac M1
* 16GB RAM
* 自分起動時のみ
* ミニゲーム系

なので8GB割り当ててもかなり現実的。

---

# おすすめ配分

## 安定重視

```bash
java -Xms4G -Xmx8G -jar paper.jar nogui
```

これかなり良い。

---

# XmsとXmxの違い

| 項目  | 意味    |
| --- | ----- |
| Xms | 最初に確保 |
| Xmx | 最大使用量 |

---

# なぜXmsを低め推奨？

Mac側にも余裕を残したいから。

特に:

* Discord
* Chrome
* IntelliJ
* Minecraftクライアント
* OBS

同時起動すると結構使う。

---

# 実際のおすすめ

## あなた向け

### 軽〜中規模

```bash
java -Xms4G -Xmx8G -jar paper.jar nogui
```

---

## execute大量なら

```bash
java -Xms6G -Xmx8G -jar paper.jar nogui
```

---

# 逆に危険

## 例えば

```bash
-Xmx14G
```

みたいなの。

OS側が苦しくなる。

---

# 重要なのはTPS

メモリより:

```text
TPS 20維持
```

の方が重要。

---

# TPS低下の本当の原因

大体これ。

| 原因                | 多い  |
| ----------------- | --- |
| execute連打         | 超多い |
| Skript while loop | 多い  |
| 無限particle        | 多い  |
| hopper            | 多い  |
| entity大量          | 多い  |
| chunk大量           | 多い  |

---

# なのでおすすめ

## spark必須

spark

---

# 監視コマンド

```mcfunction
/tps
```

```mcfunction
/mspt
```

---

# Paperはかなり軽い

Paper は:

* Vanilla
* Forge
* Fabricサーバー

よりかなり軽い。

---

# あなたの構成だと結構向いてる

特に:

```text
ロビー
↓
ミニゲームワールド
↓
試合後リセット
```

方式は軽量化しやすい。

---

# かなり重要

## 「常時ロード」減らす

これだけで激変する。

---

# 理想

## 常時

```text
lobby
```

のみロード。

---

## 試合時のみ

```text
pvp_map
tnt_map
```

ロード。

---

# 結論

## あなたなら

### 一番おすすめ

```bash
java -Xms4G -Xmx8G -jar paper.jar nogui
```

### executeかなり多いなら

```bash
java -Xms6G -Xmx8G -jar paper.jar nogui
```

これでかなり快適ライン。


* * * * * *

その構成、かなり「Hypixel系」の王道構成です。
しかも Paper ならかなり作りやすい。

あなたのやりたい流れを整理するとこう。

---

# やりたい構成

```text id="h2a67q"
ログイン
↓
Lobby
↓
GUIでゲーム選択
↓
試合用ワールド生成
↓
全員転送
↓
ゲーム開始
↓
試合終了
↓
Lobbyへ帰還
↓
試合ワールド削除
↓
次試合で新規生成
```

これ、実はかなり理想的。

---

# メリット

## 超重要

### ① 毎回マップが初期化される

PVPやTNT RUN向き。

---

### ② 荒らされても即復旧

world削除してコピーし直すだけ。

---

### ③ メモリ効率が良い

試合中しかロードしない。

---

### ④ execute負荷減る

ロビー以外停止できる。

---

# 実際の構成

## フォルダ

```text id="m1g7oi"
server/
├ lobby/
├ templates/
│  ├ pvp_template/
│  ├ tnt_template/
│  └ spleef_template/
│
├ active/
│  ├ game_001/
│  └ game_002/
│
└ plugins/
```

---

# 動き

## ① GUI選択

コンパスクリック。

↓

## ② テンプレコピー

```text id="x6w9v7"
templates/pvp_template
↓
active/game_001
```

---

## ③ ワールドロード

```mcfunction id="h1tzxv"
/mv import active/game_001 normal
```

---

## ④ プレイヤー転送

```mcfunction id="s8n5hk"
/mvtp player game_001
```

---

## ⑤ 試合終了

```mcfunction id="1ab9a9"
/mvtp @a lobby
```

---

## ⑥ ワールドアンロード

```mcfunction id="4y8xk7"
/mv unload game_001
```

---

## ⑦ フォルダ削除

```text id="r9wzdb"
active/game_001
```

削除。

---

# 必要プラグイン

## 必須

* Multiverse-Core
* Skript

---

## 超おすすめ

* FastAsyncWorldEdit
* WorldGuard
* DeluxeMenus

---

# GUI移動方法

## 一番簡単

### DeluxeMenus

GUI開いて:

```text id="w2h7f9"
[TNT RUN]
[PVP]
[SPLEEF]
```

クリックで実行。

---

# 実はかなり重要

## 「ワールドを毎回作る」より

```text id="s2djgf"
テンプレコピー
```

が圧倒的に速い。

---

# なぜ？

Minecraftの地形生成は重い。

なのでHypixel系は:

```text id="lu35qv"
既製マップコピー
```

方式。

---

# 6〜8GBでもかなり向いてる

理由:

## 常時ロードが少ない

```text id="lmb8rv"
Lobbyのみ
```

だから。

---

# あなた向きな理由

以前の話を見ると:

* execute好き
* ミニゲーム志向
* Skript興味
* Paper
* M1 Mac

なのでこの方式とかなり噛み合う。

---

# 実装レベル

## 初級

* Multiverse
* GUI移動
* 手動リセット

---

## 中級

* Skript自動生成
* 自動削除
* 自動カウントダウン

---

## 上級

* 複数試合同時
* 自動マッチング
* パーティ
* 非同期ロード
* queueシステム

---

# おすすめ進行順

## Step1

ロビー作成。

---

## Step2

GUIでワールド移動。

---

## Step3

試合終了でロビー帰還。

---

## Step4

テンプレコピー方式。

---

## Step5

完全自動化。

---

# 実はSkriptと相性抜群

例えば:

```vb
on inventory click:
    if name of event-inventory is "Game Selector":
        cancel event

        if slot is 11:
            execute console command "startpvp"
```

みたいに簡単に作れる。

---

# 次にできること

必要なら次に:

* 「Hypixel風GUI」
* 「Skriptでワールド自動生成」
* 「試合終了→削除」
* 「テンプレコピーコード」
* 「Paperで安全にworld削除」
* 「ロビーコンパス」
* 「ゲーム開始カウントダウン」

を実コード込みで作れる。

* * * * * *

初心者なら、最初は「超シンプル構成」がかなりおすすめです。
特に今の段階では、

```text id="jlwmfi"
GUI
↓
自動ワールド生成
↓
自動削除
```

まで一気にやると、かなり複雑になります。

---

# 最初はこれが良い

## 超おすすめ構成

```text id="pjlwm4"
ロビー
↓
NPC or 感圧板 or 看板
↓
固定ワールドへ移動
↓
試合終了
↓
ロビー戻る
```

まずこれ。

---

# 理由

これだけで:

* Multiverse
* ワールド移動
* Paper
* ロビー構成
* コマンド
* gamerule
* WorldGuard

全部学べる。

---

# GUIは実は少しややこしい

特に最初は:

* inventory click
* slot管理
* cancel event
* item判定
* metadata
* title判定

が出てくる。

---

# 初心者向けおすすめ順

## Step1（超おすすめ）

### 感圧板移動

```mcfunction id="5d2rq4"
/mvtp @p pvp
```

だけ。

---

## Step2

### 看板移動

```text id="ns88lm"
[PVP]
クリック
↓
warp
```

---

## Step3

### NPC移動

かなり「サーバー感」が出る。

---

## Step4

### GUI

ここで初めてやる。

---

# 一番おすすめ

## Citizens + コマンド

Citizens

これ。

---

# 例えば

## NPC作成

```mcfunction id="z2d9wb"
/npc create PVP
```

---

## クリックで移動

```mcfunction id="rf6q6f"
/npc command add mvtp %player% pvp
```

これだけ。

---

# GUIより簡単

理由:

## GUIはイベント処理が必要

NPCは:

```text id="m4g0bh"
クリック
↓
コマンド実行
```

だけ。

---

# 初心者向け最強構成

## プラグイン

* Paper
* Multiverse-Core
* WorldGuard
* Citizens
* EssentialsX

これだけでかなり作れる。

---

# 最初の完成形

```text id="i6r5yb"
ログイン
↓
Lobby
↓
NPCクリック
↓
PVPワールド
↓
試合
↓
/spawn
↓
Lobby戻る
```

これで十分「ミニゲームサーバー感」ある。

---

# ワールド自動生成は後でOK

これは実は:

* ファイル削除
* world unload
* chunk unload
* 読み込みタイミング
* プレイヤー残留

とか絡むので少し難しい。

---

# まずは固定ワールドが良い

```text id="mynp0k"
lobby
pvp
tnt
practice
```

この固定式。

---

# それでも十分楽しい

むしろ最初は:

* マップ制作
* ギミック
* execute
* Skript

に時間使った方が楽しい。

---

# あなたに合いそうな流れ

かなりおすすめ順。

## ① Multiverse

↓

## ② ロビー

↓

## ③ NPC移動

↓

## ④ WorldGuard

↓

## ⑤ Skript

↓

## ⑥ GUI

↓

## ⑦ 自動ワールド生成

---

# GUIは後から簡単に追加できる

なので今は:

```text id="t9n7el"
NPC
```

で十分おすすめ。

* * * * * *

その方式、初心者向けとしてかなり良いです。
しかも「最初はワールド1つだけ」というのは正解寄り。

---

# 結論

## こうすれば安全

```text id="n93bl5"
Lobby は常時維持
↓
game ワールドだけ作成
↓
試合
↓
全員 Lobby に戻す
↓
game を unload
↓
フォルダ削除
↓
再作成
```

これはかなり一般的。

---

# 超重要

## 危険なのは

```text id="8h8fh5"
読み込み中ワールドを直接削除
```

これ。

かなり危ない。

---

# 安全な手順

## ① まずロビーへ戻す

```mcfunction id="r6xgwb"
/mvtp @a lobby
```

または:

```mcfunction id="ikq1r4"
/spawn
```

---

# ② ワールドアンロード

```mcfunction id="6wct1m"
/mv unload game
```

これ超重要。

---

# ③ サーバーコンソール確認

```text id="u1a4eg"
Unloaded world 'game'
```

が出ればOK。

---

# ④ その後フォルダ削除

```text id="84llw2"
/game
```

フォルダ削除。

これならかなり安全。

---

# ⑤ 再作成

```mcfunction id="6aqo4j"
/mv create game normal
```

またはテンプレコピー。

---

# 初心者なら最初はこれで十分

## 固定名方式

```text id="46v7gn"
lobby
game
```

だけ。

かなり分かりやすい。

---

# かなり重要

## 「ロビーへ戻る方法」

これ最初に整備すると楽。

---

# 一番簡単

## EssentialsX

EssentialsX

導入。

---

# ロビーで

```mcfunction id="2x0e1g"
/setspawn
```

---

# 戻る時

```mcfunction id="uxmrmo"
/spawn
```

だけ。

超簡単。

---

# おすすめ構成

## ロビー

```text id="v6aj6z"
world
```

をロビーにする。

---

## ミニゲーム

```text id="tzy7gi"
game
```

だけ。

---

# サーバー構成例

```text id="nqf72n"
server/
├ world/ ← ロビー
├ game/
├ plugins/
└ paper.jar
```

かなりシンプル。

---

# 試合終了時の流れ

## あなた向けおすすめ

### ① ロビーへ戻す

```mcfunction id="u1rks0"
/spawn
```

---

### ② 数秒待つ

プレイヤー移動安定化。

---

### ③ unload

```mcfunction id="kgm1z0"
/mv unload game
```

---

### ④ フォルダ削除

MacならFinderでもOK。

---

### ⑤ 再生成

```mcfunction id="gl7m4e"
/mv create game normal
```

---

# かなり重要な注意点

## game内にプレイヤー残ってると危険

だから:

```mcfunction id="2yl1ow"
/mvtp @a lobby
```

を先にやる。

---

# さらに安全にする

## unload後に確認

```mcfunction id="h6b5p8"
/mv list
```

で:

```text id="24axmo"
game
```

が消えてればOK。

---

# 将来的には

後で:

```text id="9azxoz"
template_game
↓
コピー
↓
game
```

に進化できる。

---

# でも今は不要

最初は:

```text id="6lwn0g"
create
↓
遊ぶ
↓
delete
```

だけで十分。

---

# あなたに今おすすめな最小構成

## plugins

* Paper
* Multiverse-Core
* EssentialsX

これだけでもかなりいける。

---

# 一番シンプルな完成像

```text id="1edgmr"
ログイン
↓
Lobby
↓
NPC or command
↓
game ワールド
↓
遊ぶ
↓
/spawn
↓
game unload
↓
削除
↓
再作成
```

これ、初心者向けとしてかなり良い構成。


* * * * * *

はい、可能です。
ただし、Paper 単体には「ワールドフォルダ削除コマンド」は基本ありません。

なので初心者向けなら、最初は:

```text id="rl8o18"
unload
↓
Finderで削除
```

が一番安全です。

---

# でもコマンド自動化も可能

方法はいくつかあります。

---

# 一番おすすめ（初心者向け）

## Skriptで削除

Skript

を使う。

---

# 例えば

```vb id="e9c4ny"
command /resetgame:
    trigger:
        execute console command "mvtp @a lobby"
        wait 3 seconds
        execute console command "mv unload game"
        wait 2 seconds
        delete directory "game"
        execute console command "mv create game normal"
```

これで:

```text id="s8rtw7"
/resetgame
```

だけでOK。

---

# かなり重要

## unload前削除は危険

これはやらない。

```text id="z03nmt"
ロード中 world 削除
```

↓

* ワールド破損
* chunk壊れる
* サーバークラッシュ

可能性ある。

---

# 安全順

## 必ずこれ

```text id="m34q1i"
全員退避
↓
unload
↓
待機
↓
削除
↓
create
```

---

# 実は「wait」が超重要

Minecraftは内部処理が少し遅れる。

なので:

```vb id="4v9gnj"
wait 2 seconds
```

かなり大事。

---

# さらにおすすめ

## createよりテンプレコピー

後々。

```text id="1a3tgu"
template_game
↓
コピー
↓
game
```

の方が高速。

---

# でも今は不要

最初は:

```text id="5qq6e6"
mv create game normal
```

で十分。

---

# ちなみにMacなら超簡単

## Finder削除

```text id="26g1k6"
server/game
```

をゴミ箱へ。

これでも全然OK。

---

# 将来的におすすめ

## 専用プラグイン

例えば:

* SlimeWorldManager
* AdvancedSlimeWorldManager

ただし初心者には少し重い。

---

# あなたに今おすすめ

## 最初

```text id="p4eh82"
/spawn
↓
/mv unload game
↓
Finder削除
↓
/mv create game normal
```

---

# 慣れたら

```text id="jlwm9t"
Skript自動化
```

これが一番自然。

---

# あなたの方向性だと相性良い

以前の話から:

* execute好き
* 自動化好き
* ミニゲーム志向

なので、後々Skriptかなりハマると思う。

特に:

```text id="h6gxyf"
試合終了
↓
自動リセット
```

はSkript超得意。

* * * * * *

基本的にはできません。
Paper では、

```text id="tpj2lw"
サーバー全体で1バージョン
```

です。

---

# つまり

例えばサーバーが:

```text id="oqnwy0"
1.21.5
```

なら、

* lobby
* pvp
* tnt

全部1.21.5仕様になります。

---

# ワールドごとに変えられないもの

| 項目               | ワールド別可能？ |
| ---------------- | -------- |
| gamerule         | ○        |
| PVP              | ○        |
| 難易度              | △        |
| inventory        | ○        |
| ワールド境界           | ○        |
| 生成設定             | ○        |
| Minecraft本体バージョン | ×        |

---

# ただし「擬似的」に近づけることは可能

---

# ① ViaVersion系

## 超有名

* ViaVersion
* ViaBackwards

---

# できること

例えば:

```text id="j5ldd8"
サーバー本体: 1.21
```

でも、

```text id="jv6xlo"
1.20クライアント
1.19クライアント
```

から接続可能。

---

# でも重要

## ワールド別ではない

```text id="i6uj4z"
接続互換
```

だけ。

---

# ② 旧バージョン風PVP

例えば:

```text id="9j3n6m"
1.8風ノックバック
```

を再現するプラグイン。

---

# 有名

* OldCombatMechanics

---

# これなら

PVPワールドだけ:

* クールダウン無し
* 旧剣戦闘

に近づけられる。

---

# ③ ワールド生成は変えられる

これは可能。

例えば:

```mcfunction id="yaxkg7"
/mv create skyblock normal -g VoidGen
```

みたいに、

* Void
* Amplified
* Flat

など変えられる。

---

# ④ データパックで変える

ワールドごとに:

* レシピ
* mob
* loot
* structure

などは結構変えられる。

---

# でも本当の意味で「別バージョン」は無理

例えば:

```text id="vt5h2h"
Lobby = 1.21
PVP = 1.8
```

みたいなのは不可。

---

# もし本当にやるなら

## サーバー分離

```text id="t3lmxg"
Lobby server (1.21)
↓
Proxy
↓
PVP server (1.8)
```

---

# 使うもの

* Velocity
* BungeeCord

---

# Hypixel系はこれ

実は:

```text id="8g5x0j"
ロビー
↓
別サーバーへ転送
```

してる。

ワールド移動じゃない。

---

# でも今の段階では不要

あなたならまず:

```text id="r30vgw"
1つのPaper
↓
複数ワールド
```

で十分おすすめ。

---

# あなた向きな構成

今かなり良いのは:

```text id="rj0u8f"
Paper
+
Multiverse
+
Skript
```

これ。

---

# 将来的にできること

後々:

* ロビー専用
* PVP専用
* ミニゲーム専用
* queue server

みたいに拡張可能。

でも最初は:

```text id="u9d7tq"
Lobby + game
```

だけでかなり楽しい。
